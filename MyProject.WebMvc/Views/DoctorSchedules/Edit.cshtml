@model MyProject.Application.DTOs.UpdateDoctorScheduleRequest

@{
    ViewData["Title"] = "Edit Work Shift";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm border-0 rounded-4">
                <div class="card-header bg-white py-3">
                    <h5 class="m-0 fw-bold text-primary"><i class="fa-solid fa-calendar-check me-2"></i>Edit Work Shift</h5>
                </div>
                <div class="card-body p-4">
                    @if (!ViewData.ModelState.IsValid)
                    {
                        <div class="alert alert-danger" role="alert">
                            <div asp-validation-summary="All" class="text-danger small"></div>
                        </div>
                    }

                    <form asp-action="Edit" method="post">
                        <div class="mb-3">
                            <label asp-for="DoctorId" class="form-label fw-bold small text-muted">Select Doctor</label>
                            <select asp-for="DoctorId" class="form-select rounded-3" required>
                                @if (ViewBag.Doctors != null)
                                {
                                    @foreach (var doc in ViewBag.Doctors)
                                    {
                                        <option value="@doc.StaffId" selected="@(Model.DoctorId == doc.StaffId)">@doc.FirstName @doc.LastName (@(string.IsNullOrEmpty(doc.Specialization) ? "Doctor" : doc.Specialization))</option>
                                    }
                                }
                            </select>
                        </div>

                        <div class="mb-3">
                            <label asp-for="WorkDate" class="form-label fw-bold small text-muted">Work Date</label>
                            <input asp-for="WorkDate" type="date" class="form-control rounded-3" required />
                        </div>

                        <div class="mb-3">
                            <label asp-for="ShiftName" class="form-label fw-bold small text-muted">Shift Name</label>
                            <select asp-for="ShiftName" class="form-select rounded-3" required>
                                <option value="Morning" selected="@(Model.ShiftName == "Morning")">Morning</option>
                                <option value="Afternoon" selected="@(Model.ShiftName == "Afternoon")">Afternoon</option>
                                <option value="Night" selected="@(Model.ShiftName == "Night")">Night</option>
                            </select>
                        </div>

                        <div class="mb-4">
                            <label asp-for="MaxPatients" class="form-label fw-bold small text-muted">Max Patients Limit</label>
                            <input asp-for="MaxPatients" type="number" min="1" max="100" class="form-control rounded-3" required />
                        </div>

                        <div class="d-flex justify-content-between">
                            <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4">Cancel</a>
                            <button type="submit" class="btn btn-primary rounded-pill px-4">Save Changes</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
